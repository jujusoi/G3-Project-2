<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/test.css">
    
</head>
<body>
    <header>
         <nav class="navbar py-4 d-flex justify-content-center">
        <div class="w-50 d-flex justify-content-between">
            <section>
                <!-- logo -->
                <img src="/assets/ReadMi-logo.png" width="auto" height="38px" alt="readmi-logo" id="readmi-logo">
            </section>
            {{#if userInfo}}
            <section>
                <!-- buttons -->
                <button type="button" id="homeBtn" class="btn btnA px-3">HOME</button>
                <button type="button" id="dashboardBtn" class="btn btnB px-3">MY ACCOUNT</button>
                <button type="button" id="loginBtn" class="btn btnB px-3">LOGOUT</button>
            </section>
            {{else}}
            <section>
                <button type="button" id="homeBtn" class="btn btnA px-3">HOME</button>
                <button type="button" id="loginBtn" class="btn btnB px-3">LOGIN</button>
                <button type="button" id="signupBtn" class="btn btnB px-3">SIGN UP</button>
            </section>
            {{/if}}
        </div>
    </nav>
    </header>
    
    <main>
        {{{body}}}
    </main>

    <footer>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    {{#if userInfo}}
    <script src="/js/mainbuttons.js"></script>
    {{else}}
    <script src="/js/loggedOutbuttons.js"></script>
    {{/if}}
</body>
</html>
